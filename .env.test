# Test Environment ConfigurationAdd commentMore actions
NODE_ENV=test
PORT=3001

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# API Configuration for Tests
API_PASSWORD=test-api-password
BEARER_TOKEN=test-bearer-token-for-api-access

# Rate Limiting (disabled for tests)
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX_REQUESTS=1000

# CORS Configuration
CORS_ORIGIN=*

# Logging
LOG_LEVEL=error