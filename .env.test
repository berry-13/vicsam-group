# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Database Configuration (Test)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=vicsam_group_test
DB_USER=test_user
DB_PASSWORD=test_password

# JWT Configuration (Test)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Authentication Configuration (Test)
API_PASSWORD=test-api-password-123
BEARER_TOKEN=test-bearer-token-xyz

# Encryption Configuration (Test)
ENCRYPTION_KEY=test-encryption-key-32-chars-long
ARGON2_MEMORY_COST=65536
ARGON2_TIME_COST=3
ARGON2_PARALLELISM=1

# Security Configuration (Test)
MAX_FAILED_ATTEMPTS=5
LOCKOUT_DURATION=1800000
SESSION_TIMEOUT=900000

# Rate Limiting (Test - More permissive)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Default Admin User (Test)
DEFAULT_ADMIN_EMAIL=admin@test.com
DEFAULT_ADMIN_PASSWORD=TestAdmin123!
DEFAULT_ADMIN_FIRST_NAME=Test
DEFAULT_ADMIN_LAST_NAME=Admin

# CORS Configuration (Test)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Logging (Test)
LOG_LEVEL=error
LOG_FILE=false

# Performance (Test)
ENABLE_COMPRESSION=false
CACHE_ENABLED=false

# Feature Flags (Test)
ENABLE_REGISTRATION=true
ENABLE_PASSWORD_RESET=true
ENABLE_EMAIL_VERIFICATION=false
ENABLE_2FA=false

# Test-specific flags
MOCK_DATABASE=true
DISABLE_RATE_LIMITING=true
FAST_PASSWORD_HASHING=true

# Download Configuration
DOWNLOAD_BASE_DIR=./dati